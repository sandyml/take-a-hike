import React from 'react';

export const FavoriteCard = ({ v }) => {
  // const { currentUser } = useSelector((state) => state.usersReducer)

  // const res = v.filter((v) => v.visited !== true);
  // console.log(res, "favorite card");
  // debugger

  return (
    <div>
      {/* {v.trailhead.name} */}
    </div>
  )
};